[project]
name = "nice-tile"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyqt5>=5.15.11",
]

[dependency-groups]
dev = [
    "qgis-plugin-ci>=2.8.9",
]

[tool.qgis-plugin-ci]
plugin_path = "nice_tile"
